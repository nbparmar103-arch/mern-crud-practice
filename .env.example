# Example environment variables for server
# Set a production MongoDB URI or use local default
MONGO_URI=mongodb://localhost/mern-crud
# OR
# DB=mongodb://localhost/mern-crud

# Allow CORS for development server (set to 1 to enable)
CORS=1

# Port to run server on
PORT=3000

# React app origin (used by Socket.IO CORS)
REACT_APP_URL=http://localhost:4200
